# 敬語クイズ（Flask）

## 1. 下载
- 方式1：Code → Download ZIP（解压）
- 方式2：git clone <你的仓库地址>

## 2. 安装与启动（Mac）
在项目文件夹里打开终端：

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python3 app.py
